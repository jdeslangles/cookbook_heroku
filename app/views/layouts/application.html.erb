<!DOCTYPE html>
<html>
<head>
  <title>Cookbook</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="banner">
  </div>
  <div class="container">
    <div class="header">
      <div class="row">
        <div class="span9">
          <h1>CookBook 2.0</h1>
        </div>
        <div class="popup_login span3">
          <% if current_user %><div class="coolrecipe logged-in">Welcome back, <%= current_user.name %>!</div><% end %>
        </div>
      </div>

      <div class="navbar">
        <nav class="navbar-inner">
          <ul class ="nav">
            <li class="active"><%= link_to("Home", recipes_path) %></li>
            <li><%= link_to("Recipes", recipes_path) %></li>
            <li><%= link_to("Ingredients", ingredients_path) %></li>
              <% if current_user %>
            <li><%= link_to('Add recipe', new_recipe_path)  %></li>
            <li><%= link_to('Add ingredient', new_ingredient_path)  %></li>
              <% end %>
            <li><%= link_to('Members', users_path)  %></li>
            <li><%= link_to('Search', new_search_path)  %></li>
            <li><%= link_to('Sign up', new_user_path)  %></li>
            <li><% if current_user %><%= link_to 'Logout', session_path(session), method: :delete, data: { confirm: 'Are you sure?' } %>
              <% else %>
              <%= link_to 'Login', login_path %>
              <% end %>
            </li>
          </ul>
        </nav>
      </div>
    </div>
      <%= yield %>
  </div>

  <footer class="container">
    <p>As of 2013/07/08, this website has the yummiest collection of recipes in the WORLD. Questions? <a href="mailto:julien@xxx.com">Contact me</a>!</p>
  </footer>
</body>
</html>
